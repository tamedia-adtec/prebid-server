gdpr:
  enabled: true
  default_value: 1
  timeouts_ms:
    init_vendorlist_fetches: 1000
    active_vendorlist_fetch: 1000
metrics:
  prometheus:
    port: 8001
compression:
  response:
    enable_gzip: true
  request:
    enable_gzip: true

